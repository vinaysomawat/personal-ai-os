# Ask Brain (Ask/Reflect/Monthly tabs + shell) and Executive Summary — AUDIT

First dedicated row-by-row audit for these two panels. Ask Brain's Decide tab already has its own full audit (`DECIDE-TAB-AUDIT.md`, untouched here except for a scope-note update). Ask Brain's Reflect/Monthly tab *content* already had a detailed audit (`Quiz-Modal-and-Advisor-Shell-AUDIT.md` Part 3, rows R1-R8) — re-verified here against the current design rather than re-litigated from scratch; rows are carried forward as UNCHANGED unless noted.

Per the governing instructions: panel z-index/shell layering (`z-[60]` full-height drawers, `z-40` backdrop) was already verified against design in an earlier ad-hoc pass per `audit/TOKENS.md`'s note, covering "Ask Brain/Executive Summary panels." Re-verified here as instructed rather than trusted blindly — **Ask Brain's layering held up (still correct); Executive Summary's did not** (see EX1 below) — the TOKENS.md note turned out to be stale for Executive Summary specifically, which was not actually a full-height drawer at all. Not re-flagging Ask Brain's z-layering as a new finding since it's confirmed still correct; Executive Summary's is a real, newly-caught regression/gap.

Status legend: MATCH / MISMATCH / MISSING / OPEN (real functionality-vs-mock tradeoff, kept).

---

## Ask Brain — shell (tab bar, width), Ask tab, Reflect tab, Monthly tab

Ask Brain's panel shell (header, drawer geometry, overlay, motion) is the same `AIAdvisorProvider` component used by the 5 generic module advisors — see `Quiz-Modal-and-Advisor-Shell-AUDIT.md` Part 2 (S1-S8) for that shared shell's audit, including S8 (Ask Brain needed 440px vs the shared default of 400px — fixed there, not duplicated here) and S7 (panel motion fix, also covers Ask Brain).

| # | Element | Design spec | Verdict | Detail |
|---|---|---|---|---|
| AB1 | Panel width | `min(440px, 100vw)` | **FIXED** | See `Quiz-Modal-and-Advisor-Shell-AUDIT.md` S8 — Ask Brain reuses the shared `AIAdvisorProvider` shell (via `BrainAdvisorTrigger.tsx`), which was hardcoded to 400px for every instance. Added an optional per-instance `widthPx` override; Ask Brain now requests 440px. |
| AB2 | Tab bar (Ask/Decide/Reflect/Monthly) | Full-bleed row directly under header, `border-bottom:1px solid var(--border)`, `padding:0 12px`, underline-indicator tabs (13px/600, 2px accent bottom-border when active, no fill) | OPEN (kept) | `BrainPanel.tsx` renders a segmented-pill tab bar (`bg-surface-2 rounded-lg p-0.5`, filled-accent active state) inside the padded scrollable body, not as a separate full-bleed chrome row. Kept consistent with the same judgment already made for the 5 generic panels' internal tabs (Part 2 S5) — this exact pill style is the established, consistent pattern for every multi-way tab toggle in the app; forking Ask Brain alone to underline tabs would make it the only inconsistent one. Not touched. |
| AB3 | Tab bar / pinned input-bar chrome region | Design places the tab bar and (for the Ask tab) the input+Send row as their own un-padded, full-bleed chrome regions — outside the `padding:var(--card-pad-lg)` scrollable content area | OPEN (kept) | `AIAdvisorProvider` only exposes a single padded scrollable body slot to portaled content (no separate un-padded header-adjacent or footer chrome region), so `BrainChat.tsx` renders its quick-prompts, message list, and input all inline within that one scrollable region — visually close but not literally docked/full-bleed like the design. Fixing this properly needs a second portal slot (footer) added to `AIAdvisorProvider`'s Context/portal API, which would also apply to Career Mentor, Money Advisor's Ask tab, and Code Mentor (all of which have the same pinned-input-bar spec) — a genuinely shared-shell change, but one whose blast radius reaches into those other modules' own View files, which are out of this pass's ownership boundary. Documented rather than force-fit. |
| AB4 | Ask tab quick-prompt chips | Design's Ask tab shows no chip row at all — only the message list | OPEN (kept) | Repo shows 4 quick-prompt chips before any message is sent. Real, working functionality preserved over literal mock-matching, same precedent already established for the Decide tab's question input/ask functionality (`DECIDE-TAB-AUDIT.md`). |
| R1-R7 | Reflect tab: Best/Worst Day cards, per-module bars + strongest/weakest tagging, bar sizing, reflection-paragraph divider | See `Quiz-Modal-and-Advisor-Shell-AUDIT.md` Part 3 | UNCHANGED (MATCH) | Re-verified every value (card padding `8px 12px`, label `10px/uppercase/0.3px`, value `13px/700/mt-2px`, per-module label `11px/700/uppercase/0.4px/mb-10px`, row `12px/mb-4px`, bar `5px/radius-3px/bg-border`, reflection `13px/1.55/border-top/pt-12px`) against the current design — all still literally identical to what Part 3 recorded. No drift, not re-touched. |
| R8 | Monthly tab: numeric score bars | Design's Monthly tab has no numeric score-bar breakdown, narrative only | OPEN (kept) | Unchanged from Part 3 — `ScoreStatsSummary` still shown above the narrative in Monthly too; real accurate data, kept. |
| M1 | Monthly "Overall" box padding | `padding: 12px 14px` | UNCHANGED (MATCH) | `py-3 px-3.5` = `12px 14px` — re-verified, correct. |
| M2 | Monthly per-module row value `margin-top` | `margin-top: 3px` | **FIXED** | Was `mt-0.5` (2px) — a 1px gap never previously compared at this granularity (Part 3's R6 only covered the border/label style, not this specific value). Corrected to `mt-[3px]` in `MonthlyReviewCard.tsx`. |
| M3 | Achievement/Mistake/Recommendation boxes | `padding: 12px 14px`, tinted bg, emoji-prefixed `11px/700/uppercase/0.4px` label, value `margin-top: 4px` | UNCHANGED (MATCH) | Re-verified — `py-3 px-3.5` and `mt-1` (4px) both correct, no drift. |

---

## Executive Summary panel

**Scope correction:** `src/features/dashboard/components/ExecutiveBrief.tsx` (the "Morning Brief" card) is a different, unrelated feature — despite the similar name, it is not the Executive Summary drawer. The actual Executive Summary panel is `ExecutiveSummaryTrigger()`, a self-contained component defined inside `src/features/planner/components/PlannerView.tsx` (its trigger button lives in Planner's header row). It does not use the shared `AIAdvisorProvider` — it manages its own open state and renders its own overlay/panel markup directly.

No design source was provided for the trigger *button* itself (the design extract begins at the panel's `sc-if`, with no visible trigger markup) — the button's own styling was left untouched.

| # | Element | Design spec | Verdict | Detail |
|---|---|---|---|---|
| EX1 | Panel geometry | Full-height right-side drawer: `position:fixed; top:0; right:0; bottom:0; width:min(460px,100vw)`, `border-left` | **FIXED** | Was a small top-right absolute dropdown (`absolute right-0 top-full mt-2 w-[380px] max-h-[75vh] rounded-xl shadow-2xl`) — the exact same anti-pattern that Part 2's S1 fixed for the 5 generic advisor panels, just never applied here. `audit/TOKENS.md`'s note claiming Executive Summary's drawer/z-layering was "already verified against design" turned out to be stale/incorrect for this component — it was never a drawer at all. Converted to `fixed top-0 right-0 bottom-0 w-[min(460px,100vw)]`, matching the drawer family now used everywhere else. |
| EX2 | Overlay | `z-40` dimming overlay (`background: var(--overlay)`) | **FIXED** | The click-catcher div existed (`fixed inset-0 z-40`) but had **no background color at all** — no visible dimming behind the panel, unlike every other drawer/modal in the app. Added `bg-overlay`. |
| EX3 | z-index layer | Backdrop `40`, panel `50` in design; app's established convention bumps full-height drawers to `z-[60]` above the shared `z-50` modal layer (see `TOKENS.md`) | **FIXED** | Was `z-50` panel (the same layer as regular modals) — re-verified against the current design as instructed rather than trusting TOKENS.md's claim, and found it did not actually match the established drawer convention. Bumped to `z-[60]`, consistent with `AIAdvisorProvider` and Ask Brain. |
| EX4 | Header | `padding:var(--card-pad-lg)`, border-bottom, title `14px/700` `"◆ Executive Summary"`, close `✕` `16px` | **FIXED** | Was `px-4 py-3` with a `sticky top-0 z-10` scroll-pinning hack (only needed because the old layout let the header scroll with the content). Changed to `p-[var(--card-pad-lg)]`; `sticky` no longer needed since header is now a natural `shrink-0` flex child above the scrollable body, matching every other drawer in the app. Title/close glyph were already correct (`◆`/`✕`), unchanged. |
| EX5 | Content wrapper | `flex:1; overflow-y:auto; padding:var(--card-pad-lg); display:flex; flex-direction:column; gap:18px` | **FIXED** | Was `p-4 flex flex-col gap-4` (16px padding, 16px gap) → corrected to `flex-1 overflow-y-auto p-[var(--card-pad-lg)] flex flex-col gap-[18px]`. |
| EX6 | Panel entrance motion | `animation: fadeIn 0.18s ease-out` | **FIXED** | Was `animate-in slide-in-from-top-2 fade-in duration-150` (a top-slide, appropriate for the old dropdown shape but not the design, which specifies fade-only like every other panel). Corrected to `fade-in duration-[180ms] ease-out`, same fix pattern as Part 2's S7. |
| EX7 | Scorecard section (label, per-item row, bar) | Label `11px/700/uppercase/0.4px/mb-10px`; row `flex justify-between 12.5px mb-4px`; bar `5px/radius-3px/bg-border` track, accent fill | UNCHANGED (MATCH) | Re-verified every value — `mb-2.5`(10px)/`text-[12.5px]`/`mb-1`(4px)/`h-[5px] rounded-[3px] bg-surface-3` all already correct. No drift, not touched. |
| EX8 | Reflection paragraph | `13px/1.6/color:text-secondary/border-top/padding-top:14px` | UNCHANGED (MATCH) | `text-[13px] leading-[1.6] border-t pt-3.5`(14px) — already correct. |
| EX9 | Spend by Category section | Label `mb-8px`; rows `flex justify-between 12.5px gap-5px` | UNCHANGED (MATCH) | `mb-2`(8px)/`gap-[5px]`/`text-[12.5px]` — already correct. |
| EX10 | Recommendations section styling | Label `mb-8px`; items `bg-surface-2 rounded-8px padding:9px 12px font-size:12px`, gap `8px` | UNCHANGED (MATCH) | `mb-2`/`rounded-[8px] px-3 py-[9px] text-xs`/`gap-2` — already correct (padding `py-[9px] px-3` = `9px 12px`, exact match). |
| EX11 | Recommendation item data shape | `{{er.module}}: {{er.text}}` — a module-name-prefixed line | OPEN (kept, first noted) | Repo instead renders `{emoji} {action} — {impact}`, a richer real-data shape (`Recommendation` type has `emoji`/`action`/`impact`, not a flat `module`/`text` pair). Same class of tradeoff as other real-data-vs-mock-shape differences already kept elsewhere in this app (e.g. Decide tab's tradeoffs list) — not forked to match the mock's simpler shape since the underlying data is real and more useful. First time this specific row has been audited (no prior file covered Executive Summary), so recorded as OPEN rather than claimed as a regression. |

---

## Summary

Ask Brain's own shell fixes (width, motion, header padding) come from the shared `AIAdvisorProvider` component and are recorded in `Quiz-Modal-and-Advisor-Shell-AUDIT.md` Part 2 (S7/S8) rather than duplicated here. Ask Brain's Reflect/Monthly tab content (Part 3) was re-verified with no drift, except one 1px `margin-top` correction on Monthly's per-module rows (M2).

Executive Summary was the larger finding: despite `TOKENS.md`'s note claiming its drawer/z-layering was already verified, it turned out to still be the small top-right dropdown anti-pattern (wrong shape, wrong width, wrong z-layer, no dimming overlay, wrong motion) — the exact same class of bug Part 2's S1 fixed for the 5 generic advisor panels months earlier, just never applied to this component since it lives in `PlannerView.tsx` rather than the shared `AIAdvisorProvider`. Converted to a proper full-height right-side drawer matching design. Its actual section content (scorecard, reflection, spend breakdown, recommendations) was already correct at the literal value level — no drift there.

Verified: `tsc --noEmit` and `eslint src/` both clean after all changes in this pass.
