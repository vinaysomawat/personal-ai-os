-- Manual project tracking (Coding page's project list) is retired — GitHub
-- push activity already drives the Dashboard's Projects score automatically,
-- so the hand-maintained project list is redundant with it.

drop table if exists projects;
